# 分析紀律:算出一個新數字之前 / 之後要做什麼

本文件不是統計教科書,是一份**事故清單轉成的檢查表**。2026-07-26 兩輪對抗性審查
(Fable agent)在同一天的工作裡確認了 9 個錯誤。逐項回頭看,**9 個裡有 8 個的錯誤位置
不在計算,而在計算之前那個沒人質疑的前提**——審查者自己的結論是「數字全部可重現到
第三位小數,問題在推論」。

因此本文件只做三件事:把那些前提列成起飛前必查、規定宣稱結果時要附什麼、
以及規定唯讀怎麼被強制。

## 0. 那 9 個錯誤與各自的前提

| 錯誤 | 沒被檢驗的前提 |
|---|---|
| 所有 IC 都沒有標準誤,還拿 +0.021/+0.034/+0.040 排名次(t 全部 <2) | 「這 90 格是獨立樣本」 |
| 1 月的 `s_price`/`s_foreign` 要到 01-30 才有族群內變異,「1 月 −0.166」實際是單日 11 格 | 「1 月的因子欄位有值」 |
| ρ(A,B)=0.52 有約 0.36 是共用權重的機械底線 | 「ρ 低就代表視角不同」 |
| 說「三月底部 72,360」,實際低點是 01-02 的 66,784 | 「資料從我查詢的起點開始」 |
| 宣稱「全程唯讀」,實際 15 支腳本有 11 支沒唯讀開啟 | 「我打算唯讀 = 我做到唯讀」 |
| H1 用整數分數排名切 1/3,93% 的格邊界平手 → 結果隨 db 列序改變 | 「對整數排名會得到確定的順序」 |
| 用 1.96 當門檻,實際誤判率 10~24%,§⑨ 印出一格假陽性 | 「1.96 就是 5% 臨界值」 |
| commit 四次宣稱 tests OK,但預設 cp950 環境下是紅的 | 「我的 shell = 預設 shell」 |
| 隔離護欄可用大小寫繞過 | 「比 abspath 就能識別檔案」 |

**兩個值得記住的元觀察**:

1. **該調用的知識當時就在手邊。** 冷啟動是我同一天寫進 `RAW_DATA_BACKFILL.md` 的;
   `s_price` 是 −2..+2 是我同一天印過的;「前瞻窗重疊會高估顯著性」是既有報告的判讀警語、
   我還引用過;cp950 是 `CLAUDE.md` 明文警告。所以問題**不是上下文太長或資訊不足,
   是該用的時候沒用**——精簡上下文對這類錯誤無效,檢查表才有效。
2. **懷疑的槍口對外沒對內。** 同一天我反覆拒絕動旋鈕(「只有一個事件」「in-sample 不算
   證據」),卻把自己算出來的 +0.034 當成可排名次的事實。對策略嚴格、對自己的量測工具
   不設防。

## 1. 起飛前檢查表(任何新的統計量,算之前先過)

1. **樣本獨立嗎?** 前瞻窗重疊、同一檔連續多日、同一天多檔——三者都會讓 `n` 遠大於
   有效資訊量。→ 先收斂成「每個交易日一個數」,再用 `stats_ci.summarize()`。
   `n=格數` 永遠不是樣本數。
2. **欄位在整個期間都有值嗎?** 尤其視窗型指標(`rs20` 需 20 日、`dist_hi60` 需 60 日)。
   → 查「該欄位在該族群內第一次出現變異是哪一天」,不是查「有沒有列」。
3. **低基數變數上有平手嗎?** `s_*` 是 −2..+2 的整數,排名切片會大量平手,而 `sorted`
   是穩定排序 → 結果取決於餵入順序。→ 用**分數門檻**界定集合,或明確指定次要排序鍵;
   然後實測「換順序餵入結果是否相同」。
4. **這個門檻在這個樣本量下成立嗎?** 1.96 是大樣本、獨立下的臨界值。
   → 用 `stats_ci.t_threshold(eff_obs)`(MC 校準,見該模組註解)。未過門檻時,
   點估計的正負與大小順序都不構成證據。
5. **基準與被測對象受到同樣處理嗎?** 成本、可成交價、期間、成員定義。
   → 例:超額用「隔日開盤」計價時,族群中位也必須同一把尺。
6. **這個指標有多少是機械的?** 兩個共用因子的策略,ρ 有一個與訊號無關的下限。
   → 把因子橫斷面打散重算一次,得到「純機械底線」,再看實際值高出多少。

## 2. 宣稱結果時要附什麼

- **數字必附誤差與樣本規模**:`±SE (t=…)`、有效獨立觀測、連續區段數。
  只寫點估計等於沒有依據(此條也寫在 `WEEKLY_REVIEW.md` §2 鐵律)。
- **宣稱測試/腳本結果時附執行環境**。反例:同一份測試在 `PYTHONUTF8=1` 下 244 綠、
  在預設 cp950 下是紅的,而 CI 是 ubuntu 永遠綠。
  → **「測試通過」必須是在預設環境跑過**;若只在特殊環境驗過,就要寫出來。
  可用 `PYTHONUTF8= PYTHONIOENCODING= python -m unittest discover -s tests` 模擬預設。
- **只斷言量過的性質。** 「全程唯讀」「不寫 db」這類話,要嘛有機制強制(見 §3),
  要嘛不要說。宣稱一個自己沒強制的性質,是那 9 個錯誤裡反覆出現的形狀。
- **要拿給使用者看的數字,先用第二條獨立寫的路徑重算。** 2026-07-26 唯一沒出錯的
  臨時分析,正是因為獨立重算了報告 §② 的 tier 超額並逐格吻合。

## 3. 唯讀怎麼被強制

任何只讀的工具與**所有**臨時分析腳本,一律:

```python
import db_ro
con = db_ro.connect("data/findmind.db")     # mode=ro + PRAGMA query_only
```

`db_ro` 順手擋掉兩個陷阱:`sqlite3.connect("typo.db")` 會**默默建一個空 db**
(唯讀模式會直接報錯);只加 `mode=ro` 仍可能被繞過,故再下 `PRAGMA query_only`。

`validate.py` 的 docstring 寫「讀 db 不寫 db」,現在由 `db_ro` 強制,並由
`tests/test_db_ro.py` 釘住——**文件裡的宣稱要有機制背書**。

## 4. 臨時腳本是整條鏈最脆弱的環節

2026-07-26 所有粗糙的錯誤幾乎都在 scratchpad:單位錯 100 倍(比率標成百分比)、
拆錯函式回傳的 2-tuple(一度誤報「四張表拿不到」)、t 值虛胖成 +3.3、
`\n` 寫成字面反斜線造成 `SyntaxError` 連帶 10 個測試失敗。進 repo 的程式碼反而乾淨。

原因清楚:臨時腳本**沒有測試、沒有審查、寫一次就丟,而輸出直接進推理、再進對使用者的
宣稱**。所以規則是:

- 一律 `db_ro.connect()`
- 產出要給使用者看的數字 → 走 §2 的第二條路徑重算
- 修 repo 檔案**不要用腳本字串替換**,用編輯工具(`\n` 那次就是腳本替換造成的)
- 值得保留的分析要進 `scripts/` 並補測試,不要長期留在 scratchpad

## 5. 測試抓不到自己的盲點

H1 的順序依賴與 1.96 門檻問題,都活過了當時的 244 個測試——因為測試編碼的是**我的
假設**:測了雜湊有效、方向已宣告,但沒測「統計量與餵入順序無關」,因為沒想到順序會有
影響。

**測試是防退步的棘輪,不是防盲點的機制。** 所以:

- 已裝好的自動閘門(不依賴下次記得)見下表,新增量測時優先做成閘門而非備忘
- **對抗性審查應是常設步驟**。兩輪審查找到的是不同類的錯誤,而 H1 那個洞若晚一週發現,
  代價就是「作廢累積的 OOS」或「帶著不可重現的判準宣布結果」二選一。
  審查者本身也會出錯(第二輪誤判我沒追還原價異常),它的價值在**不共享我的假設**,
  不在正確。

| 已裝好的閘門 | 攔住的前提錯誤 |
|---|---|
| `stats_ci.MIN_EFF_OBS`(有效獨立觀測 <3 拒絕給 SE) | 「10 個成熟日算得出 t 值」 |
| `stats_ci.t_threshold()` 分級門檻 | 「1.96 就是 5%」 |
| `hypotheses.spec_digest`(覆蓋規格 + 評估函式 + 執法程式) | 「改門檻不影響舊登錄」 |
| `fetch_daily.HISTORY_FLOOR` + `normcase` | 「我知道我在寫哪個 db」 |
| `db_ro.connect()` | 「我打算唯讀 = 我做到唯讀」 |
| `tests/test_entry_docs_sync.py` | 「兩份入口文件一致」 |
