# strong-weak-scanner · 台股汰弱留強掃描

針對台股半導體與 AI 供應鏈的相對強弱研究系統。現行 universe 由
[`config/universe.csv`](config/universe.csv) 定義，共 121 檔、11 個族群：被動元件、
功率元件、封測、記憶體、矽智財、半導體設備、半導體材料、散熱、PCB/CCL、電源供應、
伺服器組裝/機構。

- **儀表板**：<https://dennisliuck.github.io/strong-weak-scanner/>
- **設定來源**：[`config/groups.csv`](config/groups.csv)、
  [`config/universe.csv`](config/universe.csv)、[`config/candidates.csv`](config/candidates.csv)
- **版本沿革與實證依據**：[`CHANGELOG.md`](CHANGELOG.md)

核心每日管線只使用 Python 3.12 標準庫與 SQLite。原始資料、正式 OOS 快照、儀表板歷史頁
與驗證報告都保留在 repo，讓每次發布可以追溯。質化證據包的 PDF 驗證與轉圖另需 Poppler。

## 方法論

> **先用籌碼聚合判斷資金正在佈局哪個族群，再用價格相對因子在族群內選強汰弱。**

這個拆分是系統最重要的邊界：外資或投信在整個市場的流向，適合回答「哪個族群受關注」；
同一族群內誰較強，主要交給相對強弱與修正日抗跌。排名結果只代表同業相對位置，不能反推
原始值一定為正，也不代表未來股價方向。

| 層級 | 回答的問題 | 現行訊號 | 輸出 |
|---|---|---|---|
| 個股層 | 同族群裡誰強、誰弱 | 七因子分數、3 日平滑、分層確認 | composite 與 tier |
| 族群層 | 哪個供應鏈正在被佈局或領漲 | 外資廣度、修正日中位買賣、相對動能、距高 | 族群狀態，不改個股分數 |
| 大盤層 | 目前是否進入修正環境 | 含息報酬指數距 20 日高 | regime，不改個股分數 |
| 觀察與研究層 | 數字如何形成、公司在做什麼、市場在傳什麼 | 官方資料解剖、技術/基本面、TDCC、借券、研究筆記、領先假說、台積電事件 | 閱讀背景，不計分 |

### 個股層：族群內七因子

[`scripts/score.py`](scripts/score.py) 的 CONFIG 是個股策略唯一旋鈕來源。價格、抗跌、外資、
投信與修正日買賣先在同族群內排五分位，得到 −2～+2 分；有效樣本少於 4 檔時不排名。
外資、投信、修正日買賣另有雜訊死區，避免全族群都接近零時仍被硬分高低。

| 因子 | 原始量與規則 | 權重 |
|---|---|---:|
| 價格相對強弱 | `rs20`＝20 日還原報酬－同族群中位報酬；族群內排名 | 1.4 |
| 修正日抗跌 | 近 20 日族群下跌日，相對同業的平均表現；至少 3 個有效日後排名 | 1.0 |
| 投信 | 近 5 日投信淨買占股本；族群內排名，±0.03% 內歸零 | 0.8 |
| 外資 | 20 日外資持股率變化；族群內排名，±0.3pp 內歸零 | 0.5 |
| 融資券 | 價格方向 × 10 日融資變化；融資減碼加分、暴增或下跌接刀扣分，並受 6%/9% 融資水位封頂 | 0.4 |
| 量能 | 當日周轉率 ÷ 自身 60 日中位；1.2～3.0 倍為 +1、低於 0.5 倍為 −1 | 0.3 |
| 修正日買賣 | 族群下跌日的外資淨買占股本；族群內排名，±0.03% 內歸零 | 0.0 |

修正日買賣目前不進 composite，但仍參與「相對蓄勢」條件與族群層聚合。每日 composite
以近 3 個交易日平均形成 `composite_s`；既有股票的 raw tier 需連續 2 日相同才正式轉層，
降低單日跳動。

分層按下表由上到下判定。括號內是資料庫使用的策略 key；儀表板使用較保守的讀者標籤。

| 儀表板分層 | 現行條件 |
|---|---|
| 相對弱勢·槓桿風險（`真弱·陷阱`） | 外資分 ≤−1、融資分 ≤−1，且 `rs20 < 0` |
| 相對強勢·過熱（`強但過熱`） | 價格分 ≥+1，且周轉率 ≥20%、量比 ≥5 倍或融資水位 ≥9% |
| 相對蓄勢（`蓄勢·外資佈局`） | 外資或修正日買賣分 ≥+2、距 60 日高 ≤−3%、抗跌不輸同業，且 `composite_s ≥ 1.5` |
| 相對強勢（`真強`） | 族群內前 2 名、`composite_s ≥ 2.5`、價格分 ≥+1，且至少一項法人分 ≥+1 |
| 相對弱勢（`真弱`） | `composite_s ≤ −3.5`，或族群倒數 2 名且分數 <0 |
| 中性觀察（`潛在/中性`） | 以上皆非；籌碼已達蓄勢前提者另列尚缺條件 |

### 族群層與大盤

[`scripts/fetch_daily.py`](scripts/fetch_daily.py) 以至少 6 檔有效樣本聚合族群指標：

- `breadth_f`：20 日外資持股率增加的成員比例。
- `med_dip`：成員在族群下跌日的外資淨買占股本中位數；目前是候選主訊號，仍在 OOS 驗證。
- `rel20`：族群 20 日中位報酬相對全 universe 中位數。
- `med_dist60`：族群成員距 60 日高的中位數。
- `breadth_t`：近 5 日投信買超的成員比例。

族群狀態同樣按優先序判定：

| 狀態 | 條件 |
|---|---|
| 蓄勢·被佈局 | `med_dip > 0` 且 `med_dist60 ≤ −5%` |
| 發動·領漲 | `rel20 > 0` 且 `med_dist60 > −5%` |
| 籌碼退潮 | `med_dip < 0` 且 `breadth_f ≤ 40%` |
| 中性觀察 / 資料不足 | 其他情況 / 樣本不足 |

大盤使用 TWSE 官方 `MI_INDEX` 的發行量加權股價報酬指數（含息）；距 20 日高 ≤−3%
時標為修正 regime。FinMind 同口徑 TAIEX 只做逐日交叉驗證，官方缺值時才作備援。
含息口徑可避免除息季的機械性下跌被誤判為市場修正，與個股使用還原價的原則一致。

### 明確不計分的內容

- `chip_health` 是七個籌碼方向的描述性診斷，不是族群內排名；其中 TDCC 大戶、股東人數、
  借券三項仍待 OOS 累積後裁決。交易所處置/注意旗標會直接標成待觀察，但不改 composite。
- MA5/20/60、RSI14、價量關係與穿越事件只描述個股相對自身歷史。
- 官方資料「數據解剖」展示成交筆數、法人買賣兩側、融資券流量、外資限額、借券拆分與
  相對所屬市場報酬指數；個股與族群版本都不計分。
- 月營收、財報、質化筆記、領先假說與研究中心的台積電法說事件都是研究背景，不直接餵入 tier。

## 資料、重算與發布邊界

### 資料來源

| 類別 | 來源與用途 |
|---|---|
| 每日五張原始表 | TWSE/TPEx 全市場批次：價格、法人、融資券、外資持股、借券；另抓處置/注意公告 |
| 還原與市場序列 | FinMind 提供除權息/分割事件；TAIEX 含息序列以 TWSE 官方 `MI_INDEX` 為正式主來源，FinMind 同序列作交叉驗證／官方缺值備援；TPEx 報酬指數仍只供觀察 |
| 週/月/季資料 | TDCC 股權分散週快照；FinMind 月營收、損益表、資產負債表、現金流量表 |
| 研究資料 | 公司 IR、MOPS、TWSE/TPEx 文件與人工維護的質化筆記、領先假說、事件錨點 |

每日價格因子使用本地倒推的 `price_adj`。除權息與分割有事件資料可重算；減資參考價未涵蓋，
以「無事件大幅跳空」警告兜底。TDCC 只提供最新一週，週五快照到次週一才生效，避免前視。

### SQLite 分層

| 層 | 主要內容 | 更新語意 |
|---|---|---|
| 原始層 | `price`、`inst`、`margin`、`holding`、`sbl`、`risk_flags`、`market`、`market_provenance`、`market_index`、`tdcc_holding`、財報與 `ref_*` | 依主鍵冪等補缺；成功來源可先 checkpoint；大盤 canonical 保留逐日來源與雙邊原值 |
| 衍生層 | `price_adj`、`daily_metrics`、`observation_metrics`、`daily_scores`、`chip_health`、`group_metrics`、`market_daily` | 依目前規則全量重建，屬 restated history |
| OOS as-seen 層 | `oos_snapshot_runs`、`oos_signal_snapshots`、`oos_group_snapshots`、`oos_market_snapshots` | append-only，不覆寫舊發布 |
| 發布層 | `index.html`、`research.html`、`archive/<資料日>.html`、`reports/` | 首頁與研究中心可重建；同日 archive 首次建立後不覆寫 |

衍生表回答「把現行規則套回歷史會得到什麼」；正式 OOS 回答「當天第一次發布時，使用者
實際看到了什麼」。同一資料日若修復後重跑會新增快照，驗證固定採最早正式發布版；只有 HTML
舊頁、沒有機器快照的日期，不得事後拼成 OOS。

### 自動化流程

所有時間皆為台灣時間：

```text
平日 18:07  價格 + 法人 raw checkpoint（不重算、不發布）
平日 19:07  再次 checkpoint；若 Actions 延遲到 23:40 後才啟動，直接正式補完
平日 21:47  提前排隊；23:40 前啟動仍只做 checkpoint，延遲跨過門檻則正式補完
       ↓
平日 23:47  TDCC → 五表終版補完 → 衍生指標 → score
             → 正式 OOS snapshot → index + immutable archive → commit

週六 09:00  validate.py → reports/validate_<資料日>.md
每月/每季   fetch_financials.py → 月營收與財報四表（不進評分）
```

正式晚場同時要求台北 23:40 終版時間門檻，以及五張原始表、universe、評分與
族群資料完整，兩者都通過才發布。任一交易所來源失敗時，
已成功部分會先寫入 SQLite 並保存 checkpoint，但工作流保持失敗，停止 score、OOS 快照與網站更新。
晚間 workflow 以 runner 的 UTC 日期鎖定資料目標日；因此排程即使延遲跨過台北午夜，
仍會補原交易日，不會誤把隔日當成當日 final pass。
所有會寫回 `main` 的工作流共用同一 FIFO concurrency group，並在實際開始時 checkout
最新 `main`；push/rebase 衝突一律標紅，不得以成功結束。完整場 push 後還會等待
Pages latest build 確認已部署同一 commit，失敗或 5 分鐘逾時都會標紅。

## 儀表板怎麼看

建議依頁面順序閱讀：

1. **今日重點**：先確認「資料至」日期、陳舊警示與大盤 regime，不把最新頁誤認為最新資料。
2. **最近研究**：首頁只保留正式筆記、多空小作文、市場議題三類入口；事件錨點歸入市場議題，
   點擊後在研究中心閱讀完整法說脈絡、KPI 與族群方向。新近不代表證據較強，畫面保留每篇文章
   的查核狀態與觀察層警語。第一次進入研究中心可照「先選一個系統問題 → 再讀市場議題 → 最後追
   知識圖譜」三步建立全貌；第一步直接對齊族群矩陣的問題式入口，不要求讀者先背 11 個族群名稱。
   初次載入不會自動把最近更新的文章當成已選起點：左側保留三步導覽，右側只說明同一閱讀順序，
   並以「先看族群矩陣」作為唯一主要行動；已有目標的讀者仍可直接從左側選文。這是閱讀順序，
   不是投資排名。從清單、矩陣、雷達或圖譜點開文章後，頁面與閱讀欄回到頂端，
   鍵盤焦點落在新文章標題；桌機預設進入專注閱讀；
   標題與查核警語後先顯示「新手閱讀任務」，但依文章類型回到不同的既有原文：一般市場議題把
   第一個「三句話抓重點」與「想一想」第一題放進「先抓住這個重點／讀完能回答」；正式筆記取
   「30 秒摘要」前 1～3 點，問題固定要求讀者分清本業、收入來源與證據邊界；多空小作文取
   「勝負手」3 點，問題固定要求比較多空各需要什麼證據。後兩種問題只套用文章類型與既有公司
   名稱，不讀正文生成結論。事件錨點若沒有上述結構化來源就不生成任務卡，避免從長文推寫新主張。
   任務卡會先顯示主要閱讀行動，再把符號、名詞與背景脈絡留在後方的選用說明，避免新手還沒開始
   閱讀就先穿過三個輔助區。780px 以下另把「開始讀三句重點／先看產業角色」排在第一句重點後，
   完整「讀完能回答」反思題保留在同卡、排在行動之後；桌機仍以兩欄同時比較重點與問題。這讓
   390px 首屏先出現一個可開始的 44px 行動，又不刪除或改寫原問題、reading mission 與來源。
   市場議題的「開始讀三句重點」直接把鍵盤焦點送到同篇三句重點；正式
   筆記與多空小作文因沒有這層新手原文，主行動先送到「產業角色」，角色卡底部再以「接著讀
   30 秒摘要／接著讀多空觀點」送回同篇來源段落，避免一按就跳過公司位於哪一層。若文章沒有可用
   的正式族群指南，則安全退回原來源段落。文末提示仍直接回到「30 秒摘要／勝負手／三句話抓重點」。一般市場議題原本
   較長的「為什麼重要」開場仍完整保留在可展開說明；兩句若命中同篇名詞小字典或研究中心共通語，
   卡片才顯示預設收合的「先認得這兩句的 N 個詞」，完全沒有命中時不顯示空提示。任務重點若
   出現 `[S1]` 來源編號、多空文章的 `H1` 假說編號、`Q1–Q4`、`2026H1`、MOPS，或
   `Universe／serverodm` 等研究中心分類名稱，會另顯示預設收合的「先解碼這段的 N 種符號」。
   每項只解釋符號、期間與分類怎麼讀，並明示不能推成證據強弱、成長、受惠或假說成立；裸 `H1`
   只在多空文章解釋為假說編號，帶年份的 `2026H1` 才解釋為上半年，避免兩者混讀。80 字以上的
   任務重點另只在既有 `；。！？` 後加入視覺停頓，原字序、來源索引與發布原文都保留。完整三句重點
   緊接在市場議題任務後，三句卡結束就直接銜接本文族群角色與所在學習階段。若該文是既有研究
   雷達候選升格而來，角色區先逐字顯示同一候選為各正式族群登錄的「本文先問」，讓讀者先把
   零件、子系統與整機責任分開；通用角色與混淆界線完整保留在一個預設收合的補充區。這組問句
   跟著 article ID，而不是跟著一次性導覽來處，所以從清單、直接連結或下一站開文也不會退回只看
   通用族群介紹；頁首仍不會虛構「由雷達進入」的返回脈絡。文章日期／閱讀時間放在這段定位之後，
   再以「再補重要性、名詞與追蹤」接回完整原文。手機已有黏性「返回學習路線」按鈕，因此隱藏重複的
   整張學習起點卡；桌機仍保留起點脈絡。「新手先讀」雖分成前後兩張讀者卡，原始內容仍依序保留為
   三句重點、為什麼重要、預設收合的名詞小字典、接下來怎麼追、想一想。市場議題的三句卡依序標成
   「先看資料 → 再補脈絡 → 最後看邊界」；這只是讀法標籤，不改寫句子、順序或證據。每句先比對同篇名詞小字典，卡片
   已列入雷達升格文章的「為什麼重要」另由作者在每段開頭寫一個粗體白話主句，例如先說情境、
   常見誤讀、責任與證據邊界；顯示層只把這個既有主句做成段落錨點並加分隔線，不從正文自動摘要。
   純可讀性改寫仍須追加同狀態 `editorial:<slug>` transition，來源、claim、monitor、證據判定與
   複核時鐘都不變。
   市場議題的主正文若一節開頭連續至少三段、而且每段第一個非空 run 都是作者明寫的粗體主句，
   讀者畫面會先列一張「本節先看」，再保留完整段落。卡片只逐字重用這些段首；遇到非段落或
   非粗體段首就停止，少於三項便不顯示，也不套用在新手導讀、研究摘要、查核附錄、正式筆記或
   多空小作文。編號只代表原文出現順序，不代表重要性、上下游或因果關係；本節名詞仍排在卡片後，
   讓讀者先建立機制差異，再查同篇原始定義。
   下方列出命中的詞並可原地展開逐字定義。若句中另有 BOM、qualification、production 等跨文章
   反覆出現的研究流程用語，才由 `config/research_reader_terms.csv` 補上明示為「研究中心共通語」
   的字面解釋與判讀邊界；同篇字典永遠優先，不拿共通語覆蓋作者原定義，也不把產業專有名詞、
   公司名或產品名自動收進來。讀者 payload 另只把 `passive`、`powersupply`、`serverodm` 等不會
   與一般技術英文混淆的內部族群 ID，顯示成正式中文族群標籤；原始帳本、連結與 machine-readable
   欄位不動。正式筆記的「研究定位與重要註記」另有更窄的明確語境：只有逐字命中
   `族群：<本文正式 group ID>` 時，才可連 `power／memory／material` 一併顯示為本文中文族群；
   同一段固定維運語 `Universe 質化參考／meta + qual_notes.py --lint／last_updated` 則顯示為
   「研究中心的公司質化參考／文章上方查核標示／更新日期」。這些轉換只發生在建立可見 DOM 時，
   原始 Markdown、runs、payload、連結、查核狀態與證據邊界都不動。沒有任何命中時不顯示空提示。
   窄於 1180px、桌機側欄大綱隱藏時，原本只出現一次的收合大綱會在新手導讀結束後改成黏性
   「閱讀位置」：持續顯示目前第幾節與同一個原始段落標題，展開後可直接跳到任一節；底線只表示
   閱讀位置，不表示研究完成度。研究查核附錄也列為最後一節。寬幅仍沿用右側 sticky 大綱，兩邊
   共用同一組 section index 與 scroll-spy，不另生成段落摘要或研究結論。此寬度若進入專注閱讀，
   隱藏大綱的 240px 網格欄也會同步移除，正文置中在最大 720px 的閱讀容器；不能只把大綱設為
   `display:none`，卻留下右側空白並把文章壓在左邊。
   正文顯示層另清除 Markdown 換行造成、可確定不屬於語意的中文空白（例如中文標點後與兩個漢字
   之間）；含漢字的 run 若以半形逗號、分號或冒號連接中文語句，也會顯示為中文標點，但英文詞列、
   數字、網址、原始 Markdown、發布 payload、連結與粗體不動。正文段落達 120 字且已有至少兩個
   中文句末標點時，1180px 以下會在原句末加入視覺停頓；仍是同一個段落、原字序與閱讀順序，
   不是摘要、改寫或新增主張，寬幅版維持連續文章排版。
   正式筆記與多空小作文進入主正文後，每個原始段落標題下另顯示一行「這節先看」：正式筆記
   把「資本配置、護城河、驗證 KPI」等固定章節翻成這一節要檢查的問題，多空文章則分清研究定位、
   多空敘事、量化背景與 `H#` 假說各自負責什麼。這是依文章類型與原始 section heading 套用的
   固定閱讀目的，不讀正文生成摘要；原標題、段落、數字、來源、順序與發布 payload 都不改。
   市場議題已由新手段落與研究摘要帶讀，因此不重複插入這一層提示。
   市場議題的清單卡與文章頁首另由 `config/research_topic_guide.csv` 逐篇提供一個人工複核的
   中文問題，先讓讀者知道本文要解開什麼，再把原技術標題完整保留為次要的「研究題名」。這份
   導覽必須精確覆蓋所有發布中的市場議題；缺一篇或多一篇都停止建置。它只改善入口與搜尋，
   不改寫閱讀任務、topic 原文、主張、證據或查核狀態。
   研究文章清單的三種類型都採「問題先讀」：市場議題沿用上述人工中文問題，正式筆記與多空
   小作文則逐字沿用同篇既有 `readingMission.question`。原始正式筆記摘要、多空待驗命題與
   市場議題題名分別保留為次要的「原始摘要／待驗命題／研究題名」，讓讀者先知道為何要讀，
   再決定是否進入原研究內容；清單不從正文、題名或關鍵字另生成問題。點入後的文章頁首會逐字
   延續同一問題，再把 topic 題名或正式筆記／多空文章原 `readerTitle` 保留為次要的
   「研究題名／原研究頁名」，避免讀者剛選好問題就重新掉回研究分類名稱。
   市場議題若已有完整且唯一的五項研究摘要，導讀後會先顯示「這篇目前能說到哪裡」：只把同篇
   既有的「一句話結論、尚未知道、下一步看什麼」提到前方，並沿用正式主張類型與即時可信度標記。
   讀者可直接跳到完整研究摘要，或開啟同篇原有名詞小字典；缺少任一摘要欄位時不推寫、不補卡，
   正式筆記與多空小作文也不套用。完整摘要仍保留在原位置，供讀者核對目前已知與影響範圍。
   完整名詞小字典仍保留，只有需要一次查整篇詞彙時才展開；再由緊接的研究摘要用
   一張「一句話結論」主卡與「目前已知、尚未知道、對哪些族群有意義、下一步看什麼」四張驗證卡
   呈現既有文字，讓讀者先抓結論、再檢查邊界，而不是在同一層五點清單裡找標籤。摘要會先用
   「兩把尺」解釋主張類型與證據可信度：前者回答結論是由來源直接支持、由資料
   推得或仍待驗證，後者衡量主命題來源的品質、獨立性、反方證據與缺口。畫面逐篇沿用正式
   `thesis_claim_id`、effective confidence、有效來源數與獨立來源鏈數；到期降級也跟著臺北日曆日
   即時更新。兩者都不等於真假機率，更不能換算成公司訂單、受惠程度或投資排名。
   讀到正文後段若忘記術語，行動版在原字典捲出畫面後顯示左下「名詞速查」，桌機則固定放在
   本頁大綱下方；
   兩者都開啟可搜尋、可用 Escape 關閉的原生對話框，逐字重用同篇名詞小字典並返回原閱讀位置，
   不另寫一套解釋。正文各節若實際用到同篇字典詞，節名下方另列「本節先認得」詞鈕；詞鈕只比對
   該節標題、段落、清單與表格文字，點擊後直接篩出同篇字典的原始定義。沒有命中的詞就不顯示，
   也不由模型補詞、改寫定義或推導新主張。
   市場議題的一般正文表在表格前另顯示「新手讀表」：兩欄表依序提示左欄、右欄；三欄以上則把
   原欄名排成「先定位最左欄 → 再讀中間欄 → 最後核對最右欄」，要求一次讀完同一列再往下。
   欄名逐字取自該表 `th`，提示不讀 row／cell 生成摘要，也不改寫表格內容、數字或證據邊界。
   寬閱讀欄把步驟並排，section 容器不足 620px 或手機版則改成單欄順序，避免讀法本身又擠成
   難讀的小卡。
   市場議題的一般正文表若閱讀欄窄於 620px，會把每一列改排成帶原欄名的閱讀卡；因此即使桌機
   master-detail 欄位偏窄或使用手機，也能在同一張卡直接看到最右側的成熟度／證據邊界。欄名與
   內容仍逐字取自原表，寬幅專注閱讀則保留原生欄列表；查核附錄也維持可聚焦的水平表格，不把
   需要逐欄對照的 ledger 改成卡片。
   正文結束後，「從這篇接著學」先重問首屏的「讀完能回答」，讓讀者用自己的話回答；需要提示時
   才展開逐字取自同篇「30 秒摘要／勝負手／三句話抓重點」的 1～3 點內容，也能回到對應原段落。
   下一站卡同時顯示下一篇
   既有文章要回答的問題與站次；卡內另把目前站與下一站的既有 graph label、phase label 排成
   兩步「閱讀順序」，讓讀者先看懂剛讀完的概念會接到哪一層。這個交接只重用正式路線資料並
   明示不代表供應鏈、受惠或因果關係。延伸區會依自己的實際寬度排版：內容寬度不超過 620px
   時，理解檢查、下一站、公司底稿與關係圖改成單欄閱讀順序；專注閱讀有足夠寬度時才並排。
   從搜尋或篩選結果開文後再沿延伸卡換篇，不會被原條件拉回舊文；左側保留原條件並提示目前
   文章不在結果中，返回清單時仍回到最初選文。正式路線之外的一般文章推薦則直接顯示「這兩篇為什麼
   相連」：兩篇若共享具名公司就列出共同公司，否則列出共同族群；共同族群超過三個時先顯示
   三個並可展開全部；卡片接著用「讀下一篇時比較」給一個可回答的問題，提醒讀者比較公司事實、
   題材情境、多空假說或證據邊界。問題只由兩篇文章類型與同一個共同標記套用固定句型，不讀正文、
   不生成新結論。這只是把原本用來選卡的既有標記公開給讀者，不建立上下游、受惠、訂單或
   因果關係，再連到既有知識圖譜或同族群研究，幫新手把單篇內容接成學習路徑。延伸卡只使用現有文章、具名公司、宣告族群與
   可追溯圖譜關係，不會因關鍵字相似就新增供應鏈主張；新手導讀先寫中文概念，再在括號保留
   英文術語，並用「範圍、角色、成熟階段」拆解技術新聞。完整來源、主張、比較與追蹤表保留在
   可展開的研究查核附錄。研究中心的
   「研究雷達」以「下一步研究什麼」呈現，卡片直接把既有白話問題當成 heading，讓讀者先看到
   想弄懂的事情；原技術題名縮成「研究題名」脈絡，再接 2–4 個關鍵詞與下一個驗證動作。
   候選明列的 1–4 個正式族群各自附一個「在這題要回答什麼」的白話
   問句，讓讀者先分清零件、子系統、整機或製程等研究責任，再點擊定位族群矩陣的本題文章、
   族群基礎起點、已完成、最大缺口與下一步。矩陣會保留同一問句並可返回原候選；若候選已升格，
   主要行動只開同一候選的正式 article，通用族群文章降為「再讀族群基礎」，並明示完成度與財務
   缺口是族群整體盤點、不是本題答案。尚未升格的候選則誠實顯示沒有本題文章，只讓族群基礎協助
   認識角色。由雷達進入矩陣時，同一問句與本題文章會先以橫跨四欄的承接卡出現，再進入族群起點、
   完成度、缺口與下一步；避免本題問題被壓進第一欄，和族群整體盤點混成同一層。已有文章時卡片
   先提供「先讀這篇回答本題」，尚無文章時不保留空白文章欄，也不猜替代內容。這些問句只拆分研究範圍，
   不代表上下游順序，也不等於族群、公司或題材已被證實受惠。原始 `why_now`、
   下一份證據、反證條件與來源完整收在查核區。這是研究資源排序，不是投資評分。
   每張候選卡會把「研究順序 N」放在橫向說明列，緊接著明示「只排研究待辦，不是股票或
   投資排名」；不再用左側直欄壓縮窄幅正文。卡面也只把內部狀態翻成「已有文章與關係圖」、
   「補進既有研究」、「等待更多證據」或「暫緩研究」等讀者語言；原始 status、rank 與稽核
   payload 完整保留，不因顯示翻譯改變研究判定。
   已升格候選的文章按鈕會同時標出閱讀時間；進文後頁首與「從這篇接著學」末端都保留「研究
   雷達第 N 題」與同一白話問句，並再次說明 N 只是研究資源安排，不是族群受惠、股價或投資排名。
   同一候選原本為各族群寫明的「在這題要回答什麼」，在自己的升格文章裡一律依 article ID 與
   `group_id` 逐字帶到「本文先問」卡；讀者先看本篇分工，需要背景時再一次展開所有族群的平常
   角色與不能外推到哪裡。直接文章連結、一般清單入口與沿路線換站只會解析目前這篇自己的候選，
   不會沿用前一篇或前一個 origin 的問句。矩陣路徑的文章首尾另保留族群、雷達題次與同一問句，
   返回時回到原矩陣列而不是跳回通用族群入口。
   返回時恢復同一張候選卡的捲動位置與鍵盤焦點；直接文章連結與一般清單入口不顯示這個一次性來處。
   知識圖譜則用同一證據底層分開查看「公司曝險」與「產業依賴」。新手先依「供電與散熱、記憶體與
   封裝、運算與互連、公司財務案例」縮小學習路線，再選一個中心主題、切換投影視角並點關係
   回查來源；每條路線會先說明建議閱讀順序，每個中心主題也會從既有文章連結提供「先讀主題文章」
   入口。每個圖譜主題只用其第一篇既有 `article_ids` 作為一個「主題站」，所以圖譜路線數與
   文章站次一致；從深連結開啟文章時，標題下方也會顯示所屬路線、目前站次，並可原位展開完整路線。
   族群矩陣的系統問題卡與文章內的站次定位都提供可展開路線地圖：逐站先以同篇既有
   `readerQuestion` 作為白話主標，再把既有 graph 名稱降為「研究節點」脈絡，並保留閱讀時間；
   原本較精確、也較多術語的 `readingMission.question` 仍逐字放在每站可展開的「精確追問」，
   進入文章後也保留在閱讀任務與「想一想」。若文章沒有 `readerQuestion`，才安全退回原精確問題，
   不由標題或模型另寫問句。目前文章另標成「目前這站」。每站可直接開啟對應文章，
   「看這站證據關係」才會前往該站圖譜。超過一個階段的路線另以明示登錄的學習階段分組，
   例如把「供電與散熱」分成「供電、保護與元件／機櫃運作與驗證／液冷部署」；頁首、路線摘要、
   目前階段與下一站卡都共用同一份階段資料，讓讀者知道目前在建立哪一層系統理解。階段只整理
   route 原有站次，不代表上下游。多階段路線展開時只先打開目前文章所在階段；族群矩陣尚未選文
   時則先打開第一階段，其餘階段以「站數＋站次範圍」摘要收合，讀者可再用滑鼠或鍵盤逐段展開。
   這只減少一次出現在畫面上的問題數，不刪除任何站點、問題或閱讀時間。路線地圖只取 route
   `graphIds`、各 graph 第一篇既有
   `articleIds`、該文 `readerQuestion` 與 `readingMission.question`，不另寫站點結論或推導跨族群關係。
   從文章進入知識圖譜後，「目前位置」仍顯示同一階段；行動版的原生主題選單以 `optgroup`
   按階段分組，避免離開文章後又退回一串沒有章節的主題名稱。若從文章的「看產業關聯」進圖，
   圖譜首屏另顯示「文章 → 關係圖」脈絡、剛才文章與下一站；「回到剛才文章」會恢復原閱讀位置、
   焦點與族群矩陣起點，讓讀者看完一條關係後繼續原站次。直接開圖譜與一般圖譜分頁不顯示這條脈絡。
   從文章返回清單時會回到開文前的位置並把焦點送回同一張文章卡；若由深連結直接進文，則把
   對應卡捲入畫面。若由族群矩陣的族群或系統問題開啟文章，文章首尾會保留「本次學習起點」，
   下一站換文時仍延續同一脈絡，並可回到原族群選擇器或原系統問題卡；一般清單與直接深連結不會
   虛構來源。文章前段另以正式 `groups` 重用族群白話角色與混淆邊界；沒有相符升格候選時，一至
   四個角色直接用卡片同時比較，不再要求讀者切換按鈕並記住上一張；超過四個角色時先保留一張
   起點卡，其餘角色放進一次即可全部展開的原生區塊。有相符候選時則先顯示各族群的本文問句，
   通用角色卡完整收在同一個補充區。直接連結進文也能在三句重點後比較本文實際宣告的族群，再讀
   技術細節；卡片並列不代表上下游、受惠或投資排序。未宣告族群的文章則不顯示、不靠
   文字猜測。預設篩選下的「第一次來」導覽也會重新顯示，不會因曾開過文章就永久隱藏。
   讀完後，「從這篇接著學」顯示下一個主題站；末站則明示路線已讀完並帶回知識圖譜頂端。例如先
   辨識液冷產品資格，再拆迴路責任邊界。文章內的「看產業關聯」卡會先明示即將開啟的是
   「公司曝險」或「產業依賴」，節點／關係數只計同一投影，不再把兩種視角的全圖總數寫在卡上；
   卡內另逐字取一條同視角既有 edge，先顯示「誰連到什麼、關係、證據、階段」，並可展開原 edge
   的 boundary。按鈕會把同一視角與 edge 帶進圖譜，這只是讀法示範，不代表重要性或受惠排序。
   圖譜首次進入時，桌機保留四步「新手讀圖」，但把完整路線、主題、投影與證據控制收進
   「目前這張圖」；摘要直接顯示學習路線、中心主題、階段、投影視角與已選證據，讓主題說明、
   起讀動作與關係圖先進入首屏。摘要展開後仍保留在原位，讀者可以再次收合，不會打開後失去返回
   精簡檢視的控制。手機首次進入則把四步讀法與完整控制都收合；展開後主題仍用單一下拉選擇，
   不先顯示全部按鈕牆。切換路線、視角、證據或台股範圍時，摘要同步更新，不另造圖譜狀態。
   從圖譜的「先讀主題文章」或關係卡「讀完整研究脈絡」進文時，文章首尾會標出原本的圖譜主題、
   公司曝險／產業依賴視角與學習階段；按「回到這張知識圖譜」會恢復原主題、投影、已選關係與
   圖譜捲動位置，讓讀者把文章內容重新對回關係線。這個起點只存在於同一次圖譜往返；直接深連結
   或一般文章清單不會被標成來自圖譜，也不會把導覽狀態寫進研究 payload。
   每個有可見關係的主題另提供「示範讀一條證實關係」；它只挑一條既有 edge 教讀法，不代表
   關係重要性。關係清單把「關係、證據、階段」直接標在文字前，點開後依序回答「這條線現在
   怎麼讀、還不能推到哪裡、看到什麼才升級」，內容只重排該 edge 已保存的 relation、evidence、
   materiality、commercial stage、boundary 與 next trigger；「讀完整研究脈絡」也只開啟該 edge
   既有 `articleIds`，不另外生成摘要、公司連結或受惠結論。較矮的桌機畫面改由圖譜頁本身單一
   捲動，不再把完整關係解讀塞進第二個小型捲動卡；從關係清單選取時會把焦點與畫面送回新詳情。
   若讀者是從文章延伸卡進圖，每條關係解讀末端另顯示剛才文章與下一站，按下後回到原閱讀位置；
   直接圖譜入口不會顯示這個一次性返回行動。
   學習路線只是導覽分組與閱讀順序，不會新增圖譜關係、文章映射或受惠排名。線型表示證據層級，
   商業材料性由同心環距離、
   節點標籤與分組清單呈現，成熟度與供應集中度仍是不同維度。每條關係都可回到 claim、來源、
   證據邊界與下一個驗證節點。「族群矩陣」的每列先用 `config/research_group_guide.csv` 說明
   該族群在研究中心負責的角色與一個容易混淆的邊界，再提供一篇既有學習路線主文章作為族群
   起點，也能另看該族群全部文章。若讀者是由雷達問句定位，已升格候選會先顯示同一篇「本題起讀
   文章」，原族群起點改列為讀完本題後的基礎延伸；未升格候選則不虛構文章。指南逐一覆蓋
   `config/groups.csv` 的正式族群，只是讀者分類，
   不會建立上下游順序、公司認證或受惠方向；起點則優先使用把該族群列在第一位的文章，再沿用
   既有路線與站次，不是熱門度或投資排序。桌機矩陣先列四個想弄懂的問題，再提供「已知道族群
   名稱？直接查找」；兩個入口都保持可見，但順序與頁首承諾的「先選問題」一致。手機預設「從問題
   開始」，讓不熟族群名稱的讀者直接從想理解的運作問題進入，已知道族群的讀者可切換到 11 個
   正式族群。族群預覽即時重用同一份白話角色、混淆邊界與既有路線收錄情形，
   並可從第一篇開始或定位完整族群進度。完整族群進度在寬度大於 1000px 時保留四欄比較；
   781–1000px 改成 2×2 卡片，將「族群起點、已完成、最大缺口、下一步」直接重複在各格，避免
   中幅仍靠遠處表頭辨識被壓窄的欄位；780px 以下維持單欄。由雷達帶入的本題承接卡會把外距
   算進可用寬度，不再超出選取列或被外框裁切。四個系統問題逐卡列出路線主文章實際宣告的
   相關族群與第一站；同一族群出現在多張卡，只表示它參與不同系統問題。「會出現在」與問題卡
   都只沿用既有路線，不靠相似度新增關係，也不表示上下游、重要性或受惠排序。讀完後再展開
   任一「預覽 N 站路線」時，桌機會把該路線卡改成整列寬度，並按既有學習階段並排；未展開時
   仍保留四張問題卡的同屏比較，手機則維持單欄閱讀順序。這只改變預覽排版，不改寫站次或關係。
   「完成度怎麼看」，用
   「公司本業底稿 → 題材具名連結 → 財務落地」解釋三層
   研究進度，並以「族群起點／已完成／最大缺口／下一步」四欄呈現；矩陣數字不是多空評分，
   完整九欄盤點與方法說明收在查核附錄。其中題材財務影響仍區分公司整體、事業部、
   產品與單位經濟，只有能直接辨識的資料才會被視為題材貢獻，公司總營收與較寬事業部參考值
   不會被當成題材收入。
3. **族群比較**：四象限看價籌位置與 5 日位移，熱圖比較各欄名次，排行榜逐欄排序；三個視圖
   使用同一批資料並聯動高亮。相對最好不等於原始值已轉正。
4. **個股分層**：可按族群篩選、展開所有成員，並查看近 5 日已確認分層的變化。
5. **族群內個股**：搜尋或單維排序；七因子列可展開原始值、門檻量尺、權重與加權貢獻。
6. **個股抽屜**：同一處查看分數驗算、技術/基本面、籌碼健康度、官方數據解剖，以及分頁保存的
   正式筆記與領先假說。後四者都不會偷偷改變分數。

歷史日期選單讀的是當日首次建立的 archive，不會拿今天的規則或研究內容回填舊畫面。

## OOS 驗證與策略治理

[`scripts/validate.py`](scripts/validate.py) 預設以 10 個交易日後、相對族群中位報酬驗證：

- 七因子與 composite 的族群內 IC。
- 各 tier 與 tier 轉移的前瞻超額報酬。
- 蓄勢條件鏈、族群狀態與 `med_dip` 命中率。
- 市值公平性，以及 TDCC/借券等未計分觀察因子。

策略判斷只認正式 as-seen 快照的 OOS 欄。完整門檻見
[`WEEKLY_REVIEW.md`](WEEKLY_REVIEW.md)：不因單日、單週或 in-sample 結果調參；每次最多改
1～2 個旋鈕。若更動權重或 tier 條件，必須同時把 `validate.py` 的 `IS_CUTOFF` 更新為當天，
並在 `CHANGELOG.md` 記錄報告、指標與決策依據。

## 本地使用與維運

所有 session 先 `git pull`，Python 使用 3.12（python.org 安裝，PSF 簽章）。正式晚場還需要
`FINMIND_TOKEN`；可選配 `FINMIND_TOKEN2`、`FINMIND_TOKEN3`，或放在已忽略的 `.mcp.json`。

```powershell
# 只讀：盤後資料鮮度、族群、分層變化與品質摘要
git pull
python scripts/daily_brief.py

# 23:40 後人工補跑完整正式管線；會發布本地 OOS snapshot，但不 commit/push
python scripts/run_daily.py

# 週度驗證、正式 DB 唯讀稽核、完整測試
python scripts/validate.py
python scripts/audit_raw_data.py
python -m unittest discover -s tests
```

`run_daily.py` 可安全重跑，只補缺口；上游未齊時會拒絕正式發布。它不會替你 review、commit
或 push。

### Daily Fetch 日誌判讀與續跑語意

- 日誌的「官方批次 `P` 次」只計五張表的 TWSE/TPEx 呼叫。完整新交易日通常為 10 次；
  早場為 4 次，早場已完成時晚場通常再補 6 次。
- `P=0` 代表指定缺口已完整而跳過，不是資料源失敗。FinMind 事件呼叫與 `market_index`
  額外官方請求會分開列示；其中 TWSE 含息報酬指數是正式大盤主來源，TPEx 指數仍是
  非阻斷觀察層。
- 一個市場成功、另一個失敗時，成功資料先落地；下次依 SQLite 缺口接續。空回應或 universe
  覆蓋不足不會被補成 0。
- 交易所價格表若明確回傳 OHLC 全空、成交量／金額／筆數皆為 0，會衍生一筆
  `trading_status=no_trade`。這類股票當日不在法人日報的有效母體內，但 price、margin、
  holding、sbl 仍須有官方列；未知或未被原始價格列驗證的缺口照常標紅。停牌日不產生
  `daily_metrics`／`daily_scores`，不推進平滑分數或 tier，復牌後的技術視窗接續前一有效交易日。
- `holding` 日內初版不視為正式終版；`--final-pass` 對當日資料有台北 23:40 硬門檻，
  23:47 排程會刷新 holding，上游資料未齊時不發布，同日重跑維持冪等。
- 完整場的 TAIEX canonical 必須精確到最新交易日：優先採 TWSE 官方含息報酬指數；
  FinMind 未發布只留下「待交叉驗證」狀態，不阻擋官方值。若 TWSE 缺值而 FinMind 已有
  同日值，明示啟用 FinMind 備援；兩邊同日值超出 `1e-6` 容許誤差則硬停，兩邊都缺也會
  在重建 `daily_metrics`／`market_daily` 前停止。Actions 會先保存五表與 TDCC checkpoint，
  不會把舊 regime 凍結成新 OOS 快照。
- schema 新增欄位的歷史 `NULL` 使用 `--backfill-expanded-fields`；只有交易所公告來源修正版、
  既有非空值也必須覆寫時才用 `--force`。

完整退出碼、請求量與 restatement 順序見
[`RAW_DATA_BACKFILL.md`](RAW_DATA_BACKFILL.md)。每日異常排查見
[`DAILY_CHECK.md`](DAILY_CHECK.md)。

## Universe 治理

Universe 每季檢視一次；[`scripts/screen.py`](scripts/screen.py) 產生現有成員體檢與候選報告，
但不自動改名單或族群。候選體檢會呼叫 FinMind（1 + 2×候選數 次），只想看既有成員時用
`--no-candidates`（零 API call、不需 token），探索性執行用 `--dry-run` 避免在 `reports/`
留下看似正式的季度報告。

| 規則 | 現行標準 |
|---|---|
| R1 業務歸屬 | 主營收 >50% 屬該族群；優先判斷公司賣自有產品，或收代工/測試/通路/工程服務費，由一手文件人工覆核 |
| R2 規模遲滯 | 候選市值 ≥50 億才可納入；既有成員跌破 30 億才建議剔除，30～50 億留在緩衝帶 |
| R3 流動性 | 候選近 20 日中位成交值 ≥3,000 萬；既有成員未達時列觀察，不自動剔除 |
| R4 冷啟動 | 候選至少有 60 個交易日 |

一檔股票只屬一個族群，跨域公司依主要商業模式與籌碼可比性歸類。`screen.py` 的業務關鍵字
檢查只負責提示疑點，不可取代 R1 人工判斷。季度檢視也應以正式質化筆記重新核對
`universe.csv` 的 `biz` 與 group：純文字過時可修 metadata；族群歸類疑義必須回到治理流程，
不得自動搬移。

### 新增族群

族群由 `groups.csv` 與 `universe.csv` 配置驅動，但仍須同時完成下列工作：

1. 維持至少 6 檔有效樣本，避免中位數與排名被少數個股主導。
2. 回補新成員原始資料，重建衍生表，並補齊質化筆記與事件錨點的 `guidance_<group>`。
3. 檢查儀表板、lint、測試與資料完整度，將治理依據記入 `CHANGELOG.md`。
4. 將回補歷史明確視為 restated；新成員/新族群的 OOS 只能從加入後第一份正式快照起算。

TDCC 只提供最新一週，新成員加入前的週資料無法補回。Universe 變更也會改變族群中位數與
所有成員排名，因此回補後的漂亮歷史不能當成策略證據。

## 質化研究筆記(`notes/qualitative/`)

正式筆記用於 R1 業務歸屬與公司研究，不進量化分數。搜尋結果、新聞與法說摘要只能協助定位；
主張必須回到公司 IR、年報/財報、MOPS 或交易所文件。

新建或重新展開完整研究一律使用 `research_profile: focused_v1`；既有未標 profile 的 v2
筆記仍依原契約有效，等下次實質重做才遷移。focused 流程的核心要求是：

- 使用 3～5 份核心一手文件，涵蓋年報、年度財報、最新季報與最新法說；年報含查核財報時
  可由同一 PDF 承擔兩個 role。
- 正文聚焦約 25～35 個重要 claim block，每個實質段落、bullet 或表格列以 `[S#]` 對到實際頁。
- 每份缺失文件最多尋找 10 分鐘；找不到就記錄 timeout、刪除未驗證主張，不用二手來源補洞。
- Drafter 建立內容定址 evidence pack；reviewer 使用同一 pack 離線重算 SHA、數字、期間、
  單位與推論邊界，不重新下載另一份文件。
- `qual_review.py` 只做唯讀 triage；HARD 項未清除不能簽核，機器命中也不等於人工驗證通過。
- 完成 `independently_verified` 後，只提交該 note 與對應 manifest，立即做成一個獨立中文 commit；
  PDF/PNG 留在 `tmp/`。

筆記品質狀態分為 `ai_draft`、`partially_verified`、`independently_verified`、`conflicted`；
內容時效與查核品質是兩條獨立軸，儀表板會分開顯示。

```powershell
python scripts/qual_notes.py --needs-review
python scripts/qual_notes.py --lint
python scripts/qual_review.py <股號>
```

逐篇查核與 evidence pack 命令見
[`QUALITATIVE_RESEARCH_RUNBOOK.md`](QUALITATIVE_RESEARCH_RUNBOOK.md)；官方文件取得順序與備援見
[`QUALITATIVE_SOURCE_ACQUISITION.md`](QUALITATIVE_SOURCE_ACQUISITION.md)。

## 領先假說（市場小作文）

[`notes/leading_hypotheses/`](notes/leading_hypotheses/) 保存市場流傳、可追溯且可證偽，
但尚未被正式一手文件完整覆蓋的主張。它不是事實認證，也不進分數。

- 只為有效 `independently_verified` 正式筆記建立，並以內容 SHA 錨定當時比較的正式版本。
- 每則保留消息發布日、實際研究收錄日、前瞻/回溯、獨立消息鏈、證據警示、生命週期、
  可證偽條件與期限；轉載同一原始事件不算多條獨立證據。
- 狀態只能由正式文件、可重算實績或事前里程碑轉移；股價與觀察層數據只可當捕捉觸發器。
- 看多/看空敘事必須各引用現有 H#、說明最脆弱處，並用 1～3 條「勝負手」交給未來資料裁決。

```powershell
python scripts/leading_hypotheses.py --lint
python scripts/leading_hypotheses.py --due
python scripts/leading_hypotheses.py --context <股號>
```

完整收錄邊界與操作見 [`LEADING_HYPOTHESES.md`](LEADING_HYPOTHESES.md) 與
[`LEADING_HYPOTHESES_PHASE2_RUNBOOK.md`](LEADING_HYPOTHESES_PHASE2_RUNBOOK.md)。

## 研究更新佇列與市場議題

`scripts/research_queue.py` 把正式筆記、逐則 H#、事件錨點、財報覆蓋、候選市場議題與
實際掃描紀錄聚成一張唯讀待辦；A–D 四個 cohort 每週輪一組，四週涵蓋全 universe。
`notes/research_topics/` 只保存「值得查」的候選議題，不是第四套正式事實庫，也不計分。

```powershell
python scripts/research_queue.py --attention
python scripts/research_queue.py --calendar --weeks 8 --output tmp/research_calendar.md
python scripts/research_queue.py --lint
python scripts/research_radar.py --lint
python scripts/research_method_audit.py --lint --baseline-ref HEAD
```

候選議題必須明列來源發布日、研究捕捉日、受影響族群／股票、route、action due 與
證據邊界；沒有新題目也要在 `scan_log.csv` 留下實際掃描範圍。已簽筆記的 `next_review`
不可單獨 snooze，否則內容 SHA 會失效。跨市場候選另記於 `notes/research_candidates/` 的
active radar，優先級、知識價值、反證與下次證據都要可機讀；只有文章與圖譜同時通過契約後
才可標為升格。完整節奏、SLA 與路由規則見
[`RESEARCH_MAINTENANCE.md`](RESEARCH_MAINTENANCE.md)。

## 事件錨點與研究中心的台積電法說

台積電 2330 是觀察層參考股，不在 universe，也不參與 `daily_metrics`、`daily_scores` 或任何
排名。每日收盤/外資持股寫入隔離的 `ref_price`、`ref_holding`；月營收與財報由獨立排程更新。

[`notes/events/`](notes/events/) 保存每季法說會等跨個股事件。每份事件必須為所有正式族群提供
`guidance_<group>`，即使未提及也明確寫 `none`；研究中心把事件發布為「市場議題」完整文章，
並保留結構化族群方向；儀表板主頁不再放置整個台積電專區。
這些方向是編輯彙整，不是預測。每季法說後更新事件日期、KPI、guidance、`next_review`，並執行：

```powershell
python scripts/qual_notes.py --lint
```

## 專案入口

| 需求 | 入口 |
|---|---|
| 盤後確認與今日討論 | [`DAILY_CHECK.md`](DAILY_CHECK.md)、`scripts/daily_brief.py` |
| 原始欄位回補/正式 DB 稽核 | [`RAW_DATA_BACKFILL.md`](RAW_DATA_BACKFILL.md)、`scripts/audit_raw_data.py` |
| 週六策略檢視 | [`WEEKLY_REVIEW.md`](WEEKLY_REVIEW.md)、`scripts/validate.py` |
| Universe 與候選 | 本頁「Universe 治理」、`scripts/screen.py`、`config/` |
| 質化筆記 | [`QUALITATIVE_RESEARCH_RUNBOOK.md`](QUALITATIVE_RESEARCH_RUNBOOK.md)、`scripts/qual_notes.py`、`scripts/qual_evidence.py`、`scripts/qual_review.py` |
| 領先假說 | [`LEADING_HYPOTHESES.md`](LEADING_HYPOTHESES.md)、[`LEADING_HYPOTHESES_PHASE2_RUNBOOK.md`](LEADING_HYPOTHESES_PHASE2_RUNBOOK.md) |
| 研究更新／市場議題 | [`RESEARCH_MAINTENANCE.md`](RESEARCH_MAINTENANCE.md)、`scripts/research_queue.py`、`scripts/research_method_audit.py`、`notes/research_topics/`、`notes/research_method_reviews/` |
| 策略與資料變更歷史 | [`CHANGELOG.md`](CHANGELOG.md) |

## 已知限制

- 券商分點資料未涵蓋；系統能看法人別，不能辨識實際分點主力。
- TDCC opendata 只有最新一週，漏抓無法事後補回；外資持股也可能受保管行重分類影響。
- 減資參考價未納入還原事件；無事件大幅跳空只能警告，仍需人工查核。
- 歷史最前段若尚無當日股本，衍生指標會以第一筆可得股本作種子；研究長窗時應注意這個邊界。
- 排名、tier、族群狀態、研究筆記與領先假說都不是報酬保證。本專案為研究工具，**非投資建議**。
