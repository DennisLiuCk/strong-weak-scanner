name: weekly-validate

on:
  schedule:
    # 台灣週六 09:00 = UTC 01:00(週五收盤資料已於週五晚間入庫)
    - cron: "0 1 * * 6"
  workflow_dispatch: {}   # 允許手動觸發(每週檢視時)

permissions:
  contents: write

concurrency:
  group: repo-main-writer
  cancel-in-progress: false
  queue: max

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: 每週容量與壓縮回復普查
        run: python scripts/audit_storage.py --gzip-probe >> "$GITHUB_STEP_SUMMARY"

      - name: 產生週度驗證報告(reports/validate_<資料迄日>.md)
        run: python scripts/validate.py

      - name: 同步首頁的證據狀態與最新週報連結
        run: |
          python scripts/build_dashboard.py
          python -m unittest -v tests.test_dashboard_sections_present.DashboardSectionsPresentTest

      - name: commit 報告(有變動才提交)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/ index.html research.html
          if git diff --staged --quiet; then
            echo "無新報告,略過 commit"
          else
            git commit -m "report: 週度驗證 $(date -u +%Y-%m-%d) [skip ci]"
            git push
          fi
